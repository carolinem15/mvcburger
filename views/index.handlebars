<h1>BURGERS!</h1>

<h2>BURGERS THAT ARE READY TO BE DEVOURED!</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>BURGERS YOU DEVOURED!</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Order a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devour it?</label><br>
    <input type="radio" name="devour" value="1" checked> Devoured!<br>
    <input type="radio" name="devour" value="0"> Still on the table!
  </div>

  <button type="submit">Order Burger</button>
</form>
